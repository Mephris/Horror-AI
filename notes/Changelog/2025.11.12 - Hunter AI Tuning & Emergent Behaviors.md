For the future, the "Wondering" time that Hunter takes to move around a patrol point should scale with the probability of a target being there.

Currently if Hunter looks at some point for a long time he wont patrol is, which is good, he does not try to patrol the points he already seen for a long time, so maybe lets just make it so its counted as visited if seen like this during wandering? or just delete HasBeenVisited to allow decisionmaking be fully on Hunter. 

The Goal for HunterBehavior is to have him patrol the points not by moving from patrol point to patrol point transform vector but him to wander around and checking patrol points as he wanders. 

### End of Day Progress

The Hunter is now patrolling the entire map effectively! The new logic (Path Cost for "Effort" + Curiosity bonus) has fixed the "stuck" loop.

We've also implemented two major "Perks" for the AI:

1. **"The Stop Sign" (Re-evaluation):** The `MoveToPatrolPoint` task is no longer "dumb." It now re-evaluates its `currentPatrolTarget` every frame. If the target becomes "cold" (e.g., the Hunter "glances" at it), the task will fail itself (`return FAILURE`), forcing the Behavior Tree to find a new, better target. This stops the AI from continuing on a stupid mission.
    
2. **"The Glance Perk" (Smart Scanning):** The `OnPatrolPointSeen` event (from the FOV) now acts as a "glance" that resets the `lastPatrolTime` for the seen point. This makes the AI _efficient_. It can visually "clear" a room from a doorway without having to walk to every single point.
    

**Tuning Notes:**

- We've added **Scaled Coldness** to the "Glance." A point seen from 5m away is "cleared" much more effectively (higher `clearAmount`) than a point seen from 25m away.
    
- We've **halved the `clearAmount`** values. This makes the AI more deliberate. A single "glance" at a "hot" point is no longer enough to cool it down, forcing the Hunter to continue its approach instead of being distracted.

## For Future
Lets make allow him to plan himself routes rather then just pick the best patrol points. :D Good luck future. 