| **Node**                   | **Logic Change**                         | **Responsibility**                                                                                                                                                   |
| -------------------------- | ---------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **`AcquirePatrolTarget`**  | **REWRITE** (New Name: `ExecuteNextJob`) | **IF** `jobQueue` is empty, **THEN** call `JobGenerator.GenerateRoomClear()`. **ELSE** pop the next job and set `currentInterestTarget` and `currentNavDestination`. |
| **`MoveToPatrolPoint`**    | **SIMPLIFIED**                           | Only moves to `currentNavDestination`. Logic is simpler now that `HunterAI` handles all path validity checks.                                                        |
| **`PerformDoorwayPeek`**   | **REWRITE** (Becomes `PerformPeekJob`)   | **OnEnter:** Sets `agent.isStopped = true`. **OnUpdate:** Counts down `Job.duration`. **OnExit:** Clears the job from the queue.                                     |
| **`IsPlayerSeen` / Chase** | **UNTOUCHED**                            | High priority interrupt remains the same.                                                                                                                            |
